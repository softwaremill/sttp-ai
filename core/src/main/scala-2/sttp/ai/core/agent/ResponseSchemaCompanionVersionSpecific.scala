package sttp.ai.core.agent

/** Scala 3-only additions to the [[ResponseSchema]] companion live in the Scala 3 counterpart of this trait; on Scala 2.13 there are none —
  * for discriminated unions, list the variants explicitly with `ResponseSchema.oneOf`.
  */
trait ResponseSchemaCompanionVersionSpecific
